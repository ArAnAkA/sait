version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: sait:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    # volumes:
    #   - ./:/usr/share/nginx/html:ro   # раскомментируйте для live-mount в dev (тогда не нужен rebuild)